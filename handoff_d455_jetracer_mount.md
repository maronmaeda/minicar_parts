# 引き継ぎ: JetRacer × RealSense D455 マウント設計

FreeCAD MCP 環境での作業継続用。ここまでの検討で確定したこと、未確定なこと、判断の根拠をまとめる。

---

## 0. このドキュメントの使い方

セクション 1〜5 が前提条件、6 が現状、7 が次にやること。
**セクション 4（未確定事項）を先に読み、実測値が入手できているかを確認すること。**
`PLATE_PITCH` が未確定のままだとタワー本体の設計に入れない。

作業スタイルの希望として、コードを書く前に寸法・荷重・固有振動数などを式で押さえてから実装に入ること。
理論が長くなる場合は、現在地と残りの手順を明示すること。

---

## 1. 背景と目的

- 対象: FaBo JetRacer Kit Race Edition（Jetson Orin Nano Super + タミヤ TT-02 シャーシ、2カメラ構成の高所カメラタワー付き）
- 目的: 既存の CSI カメラタワーを撤去し、Intel RealSense D455 を搭載するマウントを新規設計する
- 制約: 3Dプリンタ（FDM）で製作。接着ではなく**ねじで組める**こと。分割は可
- 用途: 自動運転の学習。深度 + IMU を使うため、カメラの指向安定性が精度に直結する

ユーザーの決定事項:
- **タワーは低くする**
- **タワーごと再設計する**前提でよい
- D455 の寸法は公式データシート値を信用して進めてよい（実測待ちにしない）

---

## 2. D455 の与件（データシート 337029-009 / 公式ページ）

| 項目 | 値 | 備考 |
|---|---|---|
| 外形 W×H×D | 124 × 29 × 26 mm | 公差 ±0.5。Table 3-45 |
| 平面度 | 0.2 mm | 背面当たり面の設計に影響 |
| 質量 | 約 103 g | データシートに公称値なし。実測報告値 |
| 背面ねじ | **M4 × 2、ピッチ 95 mm、背面の上下中心（底面から 14.5 mm）** | 337029-017 図 10-11 / MD0645501 Sheet 3/3 で実読。ねじ込み深さ**最大 4 mm**、推奨トルク **0.4 N·m**。仕様表の一部は M3 と誤記 |
| 底面ねじ | 1/4-20 UNC × 1（X中心＝光軸中心線上、ねじ込み深さ最大 9 mm） | |
| FOV (HD 16:9) | H 86° / V 57° | |
| IMU | Bosch BMI055、加速度 62.5/250 Hz、ジャイロ 200/400 Hz | |
| コネクタ | USB-C（**底面**。X中心から約 46.5 mm、両脇に M2 ケーブル固定ねじが約 37 / 56 mm） | **背面ではない。** 背面は全面が当たり面として使える |
| 動作周囲温度 | 0〜35 ℃ | 背面ケース温度上限 44 ℃ |

### Min-Z（解像度依存。ここは誤解しやすい）

| 解像度 | Min-Z |
|---|---|
| 1280×720 | 520 mm |
| 848×480 | 350 mm |
| 640×480 | 320 mm |
| 640×360 | 260 mm |
| 424×240 | 180 mm |

JetRacer では 848×480@60fps か 640×360@90fps を想定 → **実効 Min-Z は 0.26〜0.35 m**。

### 座標系上の重要事実

- 1/4-20 中心線から左イメージャまで **47.5 mm** = ベースライン 95 mm の半分
  → **三脚穴はカメラの X 中心線上に厳密に乗っている**。X 方向の位置決め基準に使える
- 深度ゼロ点はカメラ前面カバーガラスから **−4.55 mm**（外部標定を出すときに必要）

---

## 3. 確定した設計判断とその根拠

### 3.1 搭載高さ 130〜160 mm（光軸中心）

地面が画角下端に入る距離は `d = h / tan(V_FOV/2)`、V_FOV=57° より `d = h / 0.543`。

- h = 130 mm → d = 0.24 m
- h = 160 mm → d = 0.29 m

いずれも Min-Z（0.35 m）を下回るので、**これ以上高くしても深度性能は改善しない**。
高くする動機は「遠方の地面を斜めから見る角度が稼げる」ことだけで、重心と振動のペナルティに見合わない。

### 3.2 タワーを短くする効果（定量）

片持ち梁の固有振動数は `f_n = (1/2π)√(3EI / L³m)` なので **f ∝ L^(−3/2)**。
L を 150 → 90 mm にすると **2.15 倍**。

重心上昇は `Δh = Δm(h₁ − h₀)/(M + Δm)`。M=2.0 kg, h₀=55 mm, Δm=150 g として
- h₁ = 230 mm → Δh ≈ 12 mm
- h₁ = 140 mm → Δh ≈ 6 mm

### 3.3 タワーは箱断面にする（最重要）

既存タワーは前後に薄く左右に広い板構造で、**前後曲げ（加減速・ギャップ入力の方向）が弱軸**。
板厚 2 mm・幅 60 mm・前後間隔 25 mm・L=150 mm・E=60 GPa（CFRP）で試算すると：

- 2枚がせん断結合されていない場合: I ≈ 80 mm⁴ → **f_n ≈ 27 Hz**
- スペーサでせん断結合され箱断面として働く場合: I ≈ 3.76×10⁴ mm⁴ → **f_n ≈ 580 Hz**

27 Hz が危険な理由: タイヤ径 62 mm・車速 5 m/s のときタイヤ回転数は
`f = v/(2πr) = 5/(2π×0.031) ≈ 26 Hz` で、**アンバランス次数と共振する**。

→ **設計目標: f_n ≥ 150 Hz**（想定励振の 5 倍以上）。
→ カメラヘッドのブラケット自体が左右側板を繋ぐ横梁（シアウェブ）として働く構成にしてある。
→ さらにタワー中段とアッパーパネルを結ぶ**前後方向の斜めステー**を 1 本入れてトラス化する。

### 3.4 防振ゴムは使わない

パッシブ絶縁を効かせるには f_n を励振の 1/√2 以下（〜18 Hz）まで落とす必要があり、
今度はカメラがフラフラして外部標定が定まらない。深度 + IMU 用途では
**剛結して固有振動数を上げる**のが正解。高周波対策は当たり面に 1 mm 厚のブチルを挟む程度に留める。

### 3.5 拘束方式（改訂：逃がしをやめて剛結にした）

M4 のピッチが 95 mm と確定したので、**逃がし・長穴は全廃**した。
平面 + 2点で6自由度すべてが決まるので、これ以上の拘束は過拘束にしかならない。

| 拘束 | 部位 | 拘束する自由度 |
|---|---|---|
| 平面 | 背面プレート当たり面にカメラ背面を突き当て | Y位置、ピッチ角、ロール角 |
| M4 穴 × 2（ピッチ 95、Ø4.5 バカ穴） | カメラ背面のねじ穴に直接ねじ込む | X位置、Z位置、ヨー角 |
| 底面シェルフ 3箇所 | 荷重受け・組付け補助（拘束はしない） | — |

**1/4-20 は使わない。** 背面 2点で既に完全拘束なので、底面からも締めると
必ずどこかに無理が入り、平面度 0.2 mm の当たり面がこじれる。

USB-C が底面 |X| ≈ 46.5 にあるので、**底面シェルフは |X| ≤ 33 の範囲に限る**こと。

### 3.6 分割方針

**L字のコーナーでは分割しない。** 曲げモーメント最大の位置に継手を置くのは損。
背面プレートの裏面をベッドに置く向きにすると、シェルフもガセットも背面プレートの上に
垂直押し出しされる形になり、**サポート不要で一体printできる**。背面プレートの曲げ応力が
層内に乗るので層間剥離の弱軸も避けられる。

層間強度の検算: クラッシュ荷重を v=5 m/s・変形 20 mm で `a = v²/2s ≈ 64 G` → F ≈ 66 N。
シェルフ根元の断面係数 90×8²/6 = 960 mm³ → σ = 66×22/960 ≈ 1.5 MPa。
PETG 層間強度 20 MPa 前後に対し**安全率 10 以上**。強度面で分割する動機はない。

**分割するのはヘッドとタワーの間。** ボルトオンにすることで
- タワーを作り直してもヘッドを再印刷せずに済む
- 結合面に楔（シム）を挟んで**ピッチ角を後から変更できる**

シム角度は `Δt = H·tan θ`。ボス高さ 60 mm、−5° なら Δt = 5.25 mm。

### 3.7 樹脂へのねじ止め

直タップは禁止。**M3 熱圧入インサート**か貫通ボルト + ナイロンナット。
樹脂はクリープするので座面圧を **10 MPa 以下**に抑える。`F ≈ T/(0.2d)` で逆算：

| ねじ | 推奨トルク | 軸力 | 必要ワッシャ | 座面圧 |
|---|---|---|---|---|
| M3（インサート） | 0.3〜0.4 N·m | 約 600 N | OD 9 | 約 11 MPa |
| M4（カメラ背面） | 1.0 N·m | 約 1250 N | OD 14 | 約 9 MPa |
| 1/4-20（底面） | 1.5 N·m | 約 1500 N | OD 16 | 約 8 MPa |

普通の M3 平ワッシャ（OD 7）だと 34 MPa に達して沈むので**大径ワッシャ必須**。
**組んだ翌日と初走行後に増し締めすること**（初期クリープ分が必ず抜ける）。

---

## 4. 未確定事項 / 要実測

| # | 項目 | 用途 | 状態 |
|---|---|---|---|
| 1 | ~~左右カーボン側板の中心間距離~~ | — | **不要になった**。タワーはユーザーが自作・完成済み（60×30×100） |
| 2 | カーボン側板の板厚・内寸 | 箱断面の I 計算、結合ボス設計 | 未実測 |
| 3 | 既存タワーのねじ穴ピッチ・径 | 流用の可否判断 | 未実測 |
| 4 | アッパーパネル面から各段までの高さ | タワー長 L の決定 | 未実測 |
| 5 | フロントバンパー上端高さ | 低くしたときの遮蔽チェック | 未実測 |
| 6 | ~~D455 背面 M4 の穴ピッチ・上下位置~~ | — | **解決**。ピッチ 95 / 上下中心（データシート実読） |
| 7 | ~~D455 背面 USB-C の位置~~ | — | **解決**。USB-C は底面。背面は全面が当たり面 |
| 8 | 背面プレートを A5052 3mm 板にするか | 放熱設計。樹脂のみだと熱的に不利 | **ユーザー未回答** |

---

## 5. 見落としやすい落とし穴（既に踏みかけた3点）

1. ~~**背面プレートが USB-C を塞ぐ。**~~ → **誤り。USB-C は底面にある**（データシート図で確認済み）。
   背面は全面を当たり面に使ってよく、額縁形状にする必要はない。
   代わりに**底面シェルフが USB-C を塞ぐ**ので、シェルフは |X| ≤ 33 に限ること。
2. **背面ブラケットは放熱経路として設計されている。** Intel はアルミ 6000 系を使い接触面積を
   稼いでサーマルテープで熱を逃がすことを推奨。動作周囲温度 0〜35 ℃、背面ケース上限 44 ℃。
   樹脂のみは熱的に不利（未確定事項 #8）。
3. **IMU のサンプルレートが足りない。** 加速度 250 Hz / ジャイロ 400 Hz なので、ナイキストで
   加速度 125 Hz・ジャイロ 200 Hz までしか見えない。タップ試験は「100 Hz 以下にピークが無いこと」の
   確認には使えるが、目標の 150 Hz 以上の検証にはスマホのマイク録音かハイスピード動画を使う。

---

## 6. 現状の成果物

`d455_head_bracket.FCMacro.py` — FreeCAD マクロ（Part ワークベンチのプリミティブ + ブーリアンのみ）

生成物:
- `HEAD` — 背面プレート + 底面シェルフ + ガセット + タワー結合ボス（一体）
- `SHIM_L` / `SHIM_R` — ピッチ角シム（`SHIM_ANGLE_DEG` で角度可変）
- `CameraEnvelope_REF` — 干渉確認用のカメラ外形（印刷対象外）

主要寸法: ヘッド外形 140 × 27 × 64 mm、背面プレート厚 5、シェルフ厚 8、ガセット厚 5

**重要: このマクロは FreeCAD 上で実行検証されていない。** Python の構文チェックと
寸法の整合性計算までしか通していない。ブーリアンの失敗や API 差異でエラーが出る可能性がある。

推奨印刷設定: 背面プレート裏面をベッドに置く / サポート不要 / 外周 4〜5 本
（FDM は外周が荷重を持つ）/ インフィル 40% gyroid

---

## 7. 次のタスク（優先順）

1. **マクロを FreeCAD MCP 上で実行し、エラーを潰す。** ブーリアン失敗、`ViewObject` 周りの
   ハンドリング、`Part.Face` の平面性エラーあたりが出やすい。
2. **形状レビュー。** `CameraEnvelope_REF` との干渉、USB-C 窓の位置、シムの楔方向が
   意図どおり（負角 = 前下がり）か確認。
3. **未確定事項 #1〜#4 の実測値を反映**し、タワー本体の設計に進む。
   - 箱断面化（左右側板をスペーサでせん断結合）
   - 前後方向の斜めステーでトラス化
   - タワー長 L を決めて f_n を再計算し、150 Hz 以上を確認
4. **STEP 書き出し**（`EXPORT_STEP = True`）とスライサでの検証。
5. 試作後、タップ試験で f_n を実測（セクション 5-3 の制約に注意）。

### マクロを MCP 経由の逐次操作に置き換える場合

パラメトリック性を失わないよう、寸法は変数として保持すること。
ユーザーは「パラメータを書き換えれば全体が追従する」形を前提にしている。
Sketcher ベースに作り替える場合はスプレッドシート駆動にするのが望ましい。

---

## 8. 改訂版の成果物（2026-09-07 / rev.2）

旧 `d455_head_bracket.FCMacro.py` の HEAD（背面プレート + シェルフ + 斜めガセット一体）は**破棄**した。
背面のガセットが斜めに突き出していてタワーとの結合面が定義できておらず、
カメラ固定も「角逃がし + 摩擦」で実測待ちを前提にした逃げの設計だったため。
M4 ピッチが 95 mm と確定したので、その前提そのものが不要になった。

### 8.0 rev.1 → rev.2 で直したこと（重要）

| # | 内容 |
|---|---|
| **BUG** | 回転軸穴とクランプ穴を **XZ 平面のスケッチに描いていたため穴の軸が Y（前後）になっていた**。ボルトは X（左右）から入るので完全に機能しない穴で、タブは軸方向に中実のままだった。→ **YZ 平面のスケッチに移して X 方向に貫通**させた |
| 改良 | タブが板厚 6 mm しかなく、頬板が掴むのが **6 mm 幅の板コバだけ**だった。摩擦トルク自体は接触面積に依らないので効くが、**面圧 9.7 MPa** は樹脂のクリープ限界ちょうどで予圧が抜けて緩む。→ **14 mm 厚のボス**にして **4.0 MPa** に |
| 改良 | 回転軸をタブ厚の中心（Y = +4）に移し、カメラ重心との腕を 16 → 12 mm に短縮 |

**教訓: PartDesign では「穴をどの平面のスケッチに描くか」が穴の軸方向を決める。**
軸が X 方向の穴は必ず YZ 平面に描くこと。生成後は
`Part.makeCylinder` でボルト軸を模した円柱を作り、`common()` の体積が 0 になるか
必ず検算する（マクロの検証セクションに組み込み済み）。

### 8.1 構成

`タワー.FCStd`（＝ユーザー自作のタワー本体、60 × 30 × 100、上面 Z = 100）の中に、
**独立した 3 ボディ**として持たせた。ドッキングはすべてボルト。

| ボディ | 役割 | 質量(PETG) | 印刷姿勢 |
|---|---|---|---|
| `Body`（既存タワー） | マシンへの固定。上面に M3 インサート穴 ×4 を追加しただけ | 165 g | 変更なし |
| `ADJUSTER`（ピッチ調整） | ベース板 + 頬板2枚のヨーク。回転軸 + 円弧長穴 | 28 g | ベース底面をベッドに |
| `CAM_MOUNT`（D455 固定） | 背面プレート + 上端リブ + シェルフ3箇所 + クランプタブ | 64 g | プレート裏面をベッドに |

**どちらもサポート不要の 1 ボディ**。斜め部材は 1 本も無い。
CAM_MOUNT の突起（タブ・シェルフ・リブ）は**すべて +Y 側に揃えてある**ので、
背面プレートを寝かせれば全部が上向きに立ち上がる。

### 8.2 インターフェース（ボルト 8 本だけ）

**タワー ⇔ ADJUSTER（M3 × 4）**
- タワー上面 Z=100 に Ø4.2 × 深さ 8 の下穴 ×4（X = ±23, Y = ±10）→ **M3 熱圧入インサート**
- ADJUSTER 側は Ø3.4 バカ穴 ×4 → **M3 × 12 キャップ + 大径ワッシャ（OD 9 以上）、0.3〜0.4 N·m**

**ADJUSTER ⇔ CAM_MOUNT（M4 × 2）＝ ピッチ調整機構そのもの**
- CAM_MOUNT の**クランプタブ**（X 幅 20 / Y 厚 14 / Z 110〜151）が
  ADJUSTER の**頬板 2 枚**（内面 X = ±10.3、すきま片側 0.3）の間に入る
- **回転軸ボルト**: Ø4.5 貫通、(Y, Z) = (4, 120) → **M4 × 40 + 大径ワッシャ + ナイロンナット**
- **クランプボルト**: 頬板側は半径 16 mm の円弧長穴（幅 4.5）、タブ側は Ø4.5 の丸穴
  → **M4 × 40 + 大径ワッシャ（OD 16〜18 の平ワッシャ）+ ナイロンナット、0.8 N·m**
- **可動範囲 ±20°**。円弧長穴の端がそのまま機械的ストッパ
- 負角 = 前傾（見下ろし）。0° でカメラ光軸は水平

> 円弧長穴が「回転軸の真上」にあるのは必然。クランプ穴はタブ側では固定位置なので、
> タブが厚みを持っている方向（＝真上）にしか置けない。軸の後ろや横には作れない。

**CAM_MOUNT ⇔ D455（M4 × 2）**
- 背面 M4 × 2（ピッチ 95、Z = 165.5）を Ø4.5 バカ穴で貫通
  → **M4 × 10 + 大径ワッシャ（OD 12）、0.4 N·m**
  （プレート 6 + ワッシャ 1 = 7 mm、カメラ側ねじ込み 3 mm。**M4 × 12 以上は底突きするので不可**）
- シェルフ 3箇所（X = 中央 ±10 / 外側 ±22〜33）がカメラの自重を受ける。締結はしない
- **|X| = 33〜60 は必ず開けてある**（USB-C とケーブル固定 M2 ねじの逃げ）

### 8.3 「ピッチを決めたあと本当に止まるのか」の検算

クランプは摩擦締結。摩擦トルクは **T = 2 μ F R**（2 面 × 摩擦係数 × 軸力 × 腕）。
接触**面積**には依らないので、面積は「面圧をクリープ限界以下に抑える」ためだけに要る。

| 項目 | 値 |
|---|---|
| M4 を 0.8 N·m で締めたときの軸力 F | 1000 N（`F ≈ T/(0.2d)`） |
| 摩擦係数 μ（PETG 同士、控えめ） | 0.35 |
| クランプボルト（R = 16） | 2 × 0.35 × 1000 × 16 = **11200 N·mm** |
| 回転軸ボルト（座面等価半径 4.4） | 2 × 0.35 × 1000 × 4.4 = **3080 N·mm** |
| **保持トルク合計** | **14280 N·mm** |
| 可動側の質量（カメラ 116 g + CAM_MOUNT 64 g） | 0.180 kg |
| 重心の腕（回転軸から光軸まで） | 45.5 mm |
| **64 G クラッシュ時の要求トルク** | 0.180 × 64 × 9.81 × 45.5 = **5152 N·mm** |
| **安全率** | **2.77** |

通常走行（3 G 程度の突き上げ）に対しては安全率 60 なので、振動で少しずつ動くことはない。

**面圧**: タブ 14 mm 厚 × 荷重が広がる範囲 18 mm ≒ 252 mm² に 1000 N → **4.0 MPa**。
樹脂のクリープ限界 10 MPa に対し十分。rev.1（板コバ 6 mm）は 9.7 MPa で限界ちょうどだった。

**ワッシャ座面**: OD 16 の平ワッシャで 1000 N なら約 5.6 MPa。
普通の M4 平ワッシャ（OD 9）だと 21 MPa で必ず沈むので**大径ワッシャ必須**。

### 8.4 剛性の検算

| モード | 有効 I | L | f_n | 判定 |
|---|---|---|---|---|
| 背面プレートの前後曲げ（リブ無し） | 576 mm⁴ | 37.5 mm | 169 Hz | 目標 150 Hz に対し余裕なし |
| 同（**上端リブ追加後**） | 3126 mm⁴ | 37.5 mm | **393 Hz** | ○ |
| ヨーク頬板の前後曲げ | 13824 mm⁴ | 58.5 mm | **244 Hz** | ○ |
| タワー本体（コの字部が効き幅） | 45000 mm⁴ | 100 mm | **164 Hz** | △ **系全体の最弱点** |

E = 2.0 GPa（PETG）。上端リブ（Y +3〜+11、Z 181〜188 の 8 × 7 断面）は
追加質量 11 g で I を 5.4 倍にする。板厚を 6 → 8 mm に増やすより効率が良い。

**タワー本体が 164 Hz で系の最弱点**。目標 150 Hz は満たすが余裕は 1.1 倍しかない。
下側のコの字スロット部（脚が 10 mm × 2 枚）が効いているので、
振動が問題になったらここにスペーサを渡して箱断面化するのが最も効く（3.3 節と同じ理屈）。

### 8.5 座標系と高さ

- 原点はタワー底面の中心。+Z 上、+Y がカメラの向き（車両前方）
- 回転軸は (Y, Z) = (4, 120)
- 光軸高さ = **Z 165.5 mm**（タワー底面基準、ピッチ 0° のとき）
- ±20° 振ると光軸高さ 158.7〜167.1 mm、カメラ前端 Y = 16.9〜48.0 mm
- タブ下端の最低点は Z = 108.2 mm（ベース板上面 106 に対し 2.2 mm クリア。全角度で確認済み）

**光軸を下げたい場合**は `PIV_Z` と `CAM_Z0` を下げる。下限は
`PIV_Z ≥ BASE_Z1 + 12 + 2 = 120`（タブ下端の角がベース板に当たる）。
それ以上はタワー自体を短くすること。

### 8.6 角度の出し方

印刷した目盛りは精度が出ないので付けていない。代わりに
**CAM_MOUNT 上端リブの天面が光軸と平行な基準平面**になっている。
ここにスマホの傾斜計を載せて角度を読むのが最も正確で早い。
0° はこの面が水平になったとき。

### 8.7 ファイル

| ファイル | 内容 |
|---|---|
| `タワー.FCStd` | 本体。3 ボディ + D455 参照外形 + ボルト参照。**スケッチは全て残してある** |
| `jetracer_d455_mount.FCMacro.py` | 生成マクロ。冒頭の PARAMETERS を書き換えて再実行すれば全体が追従 |
| `export/*.step`, `export/*.stl` | 各ボディ単体 + アセンブリ |
| `export/joint_iso.png`, `joint_side.png` | 連結部の説明図（ボルトを赤で表示） |
| `タワー.backup-*.FCStd` | 改変前のバックアップ |

マクロは**冪等**。再実行すると ADJUSTER / CAM_MOUNT / 参照物だけを作り直し、
既存タワーの `Sketch` / `Pad` / `Pocket` / `Pocket001` には触れない。
末尾に検証セクションがあり、**ボルト軸の貫通チェック**・**±20° 全域の干渉チェック**・
**クランプ保持トルクの検算**を毎回自動で出力する。

### 8.8 組立手順

1. タワー上面の 4 穴に M3 熱圧入インサートを入れる（はんだごて 200 ℃、垂直に押し込む）
2. ADJUSTER を M3 × 12 + ワッシャ ×4 でタワーに固定（0.3〜0.4 N·m）
3. CAM_MOUNT のタブをヨークの頬板の間に差し込み、**回転軸 M4 × 40** を通して
   ナイロンナットで**軽く**締める（この時点では回る状態）
4. D455 をシェルフに載せてから **M4 × 10 ×2**（OD 12 ワッシャ）で背面を締結、0.4 N·m
5. **クランプ M4 × 40** を円弧長穴に通し、角度を決めてから **0.8 N·m** で締める。
   そのあと回転軸ボルトも 0.8 N·m で本締め
6. **翌日と初走行後に全数増し締め**（樹脂の初期クリープが必ず抜ける）

### 8.9 残っている検討事項

- 放熱（4章 #8）は未解決。背面が全面当たり面として使えることが分かったので、
  **プレートとカメラの間に熱伝導テープを貼る**か、背面プレートだけ A5052 3 mm 板に置換する案が有効
- タワーの箱断面化（8.4 節）
- 実機でのタップ試験（5章 #3 の制約に注意）

---

## 9. ファイル構成と編集の作法（2026-09-08 追加）

### 9.1 アセンブリと部品ファイルを分けた

```
Free_cad/
├── タワー.FCStd                    ← アセンブリ（3ボディ + 参照物）
├── jetracer_d455_mount.FCMacro.py  ← 生成マクロ（パラメトリックの正）
├── split_parts.FCMacro.py          ← アセンブリ → 部品ファイルへの切り出し
├── parts/
│   ├── 01_TOWER.FCStd              ← タワー本体       130049 mm3 / スケッチ4
│   ├── 02_ADJUSTER.FCStd           ← ピッチ調整ヨーク   22125 mm3 / スケッチ5
│   ├── 03_CAM_MOUNT.FCStd          ← D455 固定治具     50665 mm3 / スケッチ6
│   └── _backup/                    ← 上書き前の自動退避
└── export/                         ← STEP / STL / 説明用 PNG
```

各部品ファイルには次が入っている。

- **その部品の PartDesign Body 一式**（スケッチ → パッド → ポケットの履歴がそのまま）
- **相手部品と D455 の参照ソリッド**（`REF_` で始まる `Part::Feature`、半透明、印刷対象外）
  当たりを見ながら単体で設計できるようにするためのもの。不要なら非表示にする

座標系はアセンブリと共通（原点 = タワー底面の中心、タワー上面 Z=100）。
部品ファイル側で座標系を動かすと当たり確認ができなくなるので動かさないこと。

> FreeCAD のドキュメント内部名は数字で始められないので、`01_TOWER.FCStd` の
> 内部名は `_01_TOWER` になる。スクリプトから引くときは名前ではなく `FileName` で
> 探すこと（`split_parts.FCMacro.py` の `check_fit()` はそうしてある）。

### 9.2 スケッチの拘束状態 — 全部「完全拘束」にした

分割前は、生成したスケッチには**閉ループの一致拘束しか入っていなかった**。
座標は正確だが、GUI で点をドラッグすると形が崩れる状態だった。

| | 分割前 | 現在 |
|---|---|---|
| `Sketch` / `Sketch001` / `Sketch002`（ユーザーが GUI で作図） | 完全拘束 | 完全拘束（触っていない） |
| `Sketch_TowerInserts` ほか生成分 12 枚 | **未拘束** | **完全拘束（DoF=0 / 冗長 0 / 矛盾 0）** |

マクロに `fully_constrain()` を追加し、
**水平/垂直 → 半径・中心座標 → 端点座標** の順に「原点からの寸法拘束」を貪欲に足して、
冗長・矛盾になったものと DoF が減らなかったものは即座に取り消す方式にした。
生成直後の座標をそのまま拘束値にするので**形状は 1 mm も動かない**
（前後で体積が完全一致することで確認済み）。

**寸法の読み方**: 拘束値はすべて「スケッチ原点からの絶対座標」で、
グローバル座標系と一致している。たとえば `Sk_A2_CheekProfile` の DistanceY = 142 は
頬板上端の Z、`Sk_A5_PivotArc` の Radius = 18.25 / 13.75 は円弧長穴の外径/内径
（= ARC_R ± ARC_W/2）というふうに直接読める。

### 9.3 「どう描いたか」の履歴について

FreeCAD が保持しているのは**フィーチャの履歴**（どのスケッチをどう押し出したか）であって、
**作図の操作履歴**（どの順に線を引いたか）は残らない。後者に相当するのは
`jetracer_d455_mount.FCMacro.py` のコードそのもので、各スケッチの構築が
`add_closed_loop()` の呼び出しとして 1 対 1 で読める形にしてある。

### 9.4 運用: どちらを「正」にするか決めること

部品ファイルで寸法を変えてもアセンブリには自動で戻らない。二重管理になるので、
最初にどちらかに決めること。

**(A) マクロを正とする**（推奨。全体の寸法連鎖を変えるとき）

1. `jetracer_d455_mount.FCMacro.py` 冒頭の PARAMETERS を編集
2. マクロを実行 → `タワー.FCStd` が更新される（末尾の検証出力を必ず確認）
3. `split_parts.FCMacro.py` を実行 → `parts/*.FCStd` を作り直す
   （既存ファイルは `parts/_backup/` に自動退避される）

**(B) 部品ファイルを正とする**（1 部品だけ細部を詰めるとき）

1. `parts/0X_*.FCStd` を開いてスケッチの寸法を直接編集
2. `split_parts.FCMacro.py` を読み込んで `check_fit()` を実行し、当たりを確認

   ```python
   exec(open("C:/Users/user/Documents/Free_cad/split_parts.FCMacro.py",
             encoding="utf-8").read())
   check_fit()   # ピッチ -20..+20 deg 全域で ADJUSTER x CAM_MOUNT の干渉体積を出す
   ```

3. **この状態で split を再実行すると編集が消える**ので、(B) に入ったら (A) に戻らないこと

パラメトリックに効くことは確認済み（`Sk_A5_PivotArc` の Radius 拘束を +2 mm したら
体積が 22125 → 21635 に追従し、戻したら 22125 に復帰した）。

### 9.5 まだ無いもの

- **TechDraw の 2D 図面**（寸法入りの製作図）。作っていない。必要なら各部品ファイルに追加できる
- **App::Link を使った本格的なアセンブリ**。現状の `REF_` は形状のコピーなので、
  部品を編集しても自動更新されない。当たり確認は毎回部品ファイルから形状を読み直す
  `check_fit()` で行うこと

---

## 10. 締結部品（ボルト・ワッシャ・ナット）の作り込み（rev.3 / 2026-09-08）

### 10.1 考え方

ねじの実物がまだ決まっていないので、**`FASTENERS` の1表だけ書き換えれば部品の
大きさが追従する**構造にした。ワッシャ外径を変えると、頬板の Y 幅もベース板の
外形もタワー取付穴の位置もカメラ高さ `CAM_Z0` も、全部自動で計算し直される。

```python
FASTENERS = {
  'base':  dict(label='M3x12', nom_d=3.0, clear=3.4, washer_od=12.0, washer_t=0.8,
                head_d=5.5, head_h=3.0, nut_af=0.0, nut_h=0.0, tool_d=0.0,  torque=0.35),
  'pivot': dict(label='M4x40', nom_d=4.0, clear=4.5, washer_od=14.0, washer_t=1.0,
                head_d=7.0, head_h=4.0, nut_af=7.0, nut_h=5.0, tool_d=14.0, torque=0.80),
  'clamp': dict(label='M4x40', nom_d=4.0, clear=4.5, washer_od=14.0, washer_t=1.0,
                head_d=7.0, head_h=4.0, nut_af=7.0, nut_h=5.0, tool_d=14.0, torque=0.80),
  'cam':   dict(label='M4x10', nom_d=4.0, clear=4.5, washer_od=12.0, washer_t=1.0,
                head_d=7.0, head_h=4.0, nut_af=0.0, nut_h=0.0, tool_d=0.0,  torque=0.40),
}
SEAT_MARGIN = 1.5      # ワッシャ外周から部品の縁までの最小残り
ENGAGE = {'cam': 4.0, 'base': 6.0}   # めねじへのねじ込み深さ
```

### 10.2 逆算される寸法

| 導出 | 式 | 現在値 |
|---|---|---|
| 頬板の前端 `CHEEK_Y1` | `PIV_Y + ARC_R·sin(SPAN) + 座金R + 余白` | 17.97 |
| 頬板の上端 `CHEEK_Z1` | `PIV_Z + ARC_R + 座金R + 余白` | 144.5 |
| 取付穴 `FIX_X` | `頬板外面 + max(座金R, 工具R) + 余白` | 23.80 |
| ベース板 `BASE_HW / BASE_HD` | 頬板と座金を包む最小値（タワー以上） | 31.3 / 18.97 |
| カメラ底面 `CAM_Z0` | 回転時に後下角が頬板上端に当たらない高さを逆算 | 152.0 |

結果、**ベース板は 62.6 × 37.9 mm**（タワー 60 × 30 に対して X で片側 1.3、
Y で片側 4.0 のはみ出し）、**光軸は Z 166.5 mm** になった。

### 10.3 マクロが毎回自動で検査すること

末尾の検証セクションが以下を全部チェックし、1つでも外れると `NG` を出す。

| 検査 | 内容 |
|---|---|
| ボルト軸の貫通 | 軸を模した円柱と各部品の `common` が 0 か |
| **ワッシャ座面** | ワッシャ外周 1 mm 幅のリングが、**全ピッチ角で**母材から食み出さないか |
| **頭 / ナット / 工具** | 実形状（六角柱含む）+ 工具回転径の空間が、全ピッチ角で他部品と当たらないか |
| ねじ込み深さ | D455 は 4 mm 以下、タワーインサートは下穴深さ以下 |
| 座面圧 | 各ワッシャ座面が樹脂のクリープ限界 10 MPa 以下か |
| クランプ保持トルク | 64 G に対する安全率 ≥ 1.5 |

現在は全項目 `ALL OK`。座面圧は clamp 7.2 / base 5.6 / cam 5.1 MPa、
クランプ安全率 2.74。

### 10.4 この検査で見つかった実際の不具合

1. **座面圧が超過していた**。clamp は OD12 ワッシャで 10.3 MPa、base は OD9 で
   10.7 MPa。どちらもクリープ限界ちょうどで、予圧が抜けて緩む。
   → clamp/pivot を **OD14**、base を **OD12** に変更（OD12 のまま使うなら
   clamp のトルクを 0.7 N·m 以下に落とす。検証がそう教えてくれる）
2. 検査式そのもののバグ 2 件（座面判定が長穴をまたぐ分まで食み出しに数えていた／
   クランプ位置の角度式の符号が逆）。前者は「穴を開ける前の素材ブロック」を
   基準にすることで解決した

### 10.5 ねじの調達メモ

| 用途 | 本数 | ねじ | ワッシャ | ナット | トルク |
|---|---|---|---|---|---|
| タワー ⇔ ADJUSTER | 4 | M3 × 12 キャップ | 平 OD12（M3 大径） | — （タワー側は M3 熱圧入インサート） | 0.35 N·m |
| 回転軸 | 1 | M4 × 40 | 平 OD14 × 2 | M4 ナイロンナット | 0.8 N·m |
| クランプ | 1 | M4 × 40 | 平 OD14 × 2 | M4 ナイロンナット | 0.8 N·m |
| D455 背面 | 2 | **M4 × 10**（12 以上は底突き） | 平 OD12 | — | 0.4 N·m |

M4 の長さ根拠: プレート 6 + ワッシャ 1 = 掴み 7 mm、カメラ側ねじ込み 3〜4 mm
（データシート上限 4 mm）→ **10〜11 mm**。

回転軸とクランプの M4 × 40 は、頬板 6 + タブ 20 + 頬板 6 = 32.6 mm に
ワッシャ 2 枚とナイロンナットを足した長さ。

### 10.6 部品ファイルにも締結部品が入っている

`parts/0X_*.FCStd` それぞれに、ボルト・ワッシャ・ナット 8 本ぶんの参照ソリッド
（`REF_Bolt*`、赤、印刷対象外）が入っている。単体で設計を詰めるときに
座面と工具の逃げを見ながら作業できる。不要なら非表示にする。

---

## 11. 運用 (B) を正式採用 — App::Link で組立が自動追従する（2026-09-08）

**FreeCAD の GUI で部品ファイルを直接編集する運用に切り替えた。**
`ASSEMBLY.FCStd` が部品ファイルを **App::Link（外部リンク）** で参照しているので、
部品を編集して保存すると組立表示が自動で追従する。

### 11.1 ファイルの役割（ここが正）

```
Free_cad/
├── ASSEMBLY.FCStd            ★ 組立（表示・当たり確認用）。中身はリンクだけ
├── parts/
│   ├── 01_TOWER.FCStd        ★ タワー本体      ← ここを編集する
│   ├── 02_ADJUSTER.FCStd     ★ ピッチ調整ヨーク  ← ここを編集する
│   └── 03_CAM_MOUNT.FCStd    ★ D455 固定治具    ← ここを編集する
├── assembly_link.FCMacro.py     組立を作り直す / ピッチ角を与える / 照合する
├── split_parts.FCMacro.py       アセンブリ → 部品ファイルへの切り出し
├── jetracer_d455_mount.FCMacro.py  全体をゼロから生成し直すとき用
└── タワー.FCStd                 上記マクロの出力（作り直しの起点）
```

### 11.2 日常の手順

1. `parts/02_ADJUSTER.FCStd` などを開く
2. スケッチの寸法をダブルクリックして編集（全スケッチ完全拘束済み）
3. **Ctrl+S で保存**
4. `ASSEMBLY.FCStd` を開いていれば、その場で形が変わる

実測で確認した挙動:

```
BEFORE                      part= 27820.4  assembly-link= 27820.4
部品側で頬板上端 144.5 -> 150.0 に変更して部品だけ recompute
                            part= 33553.1  assembly-link= 33553.1   ← 即追従
```

**組立側の再計算すら不要**だった。`ASSEMBLY.FCStd` を単独で開くと参照先の
部品ファイル 3 つも自動で開かれ、リンクは相対パスで解決される（確認済み）。

### 11.3 ピッチ角の与え方

`ASSEMBLY.FCStd` のツリーにある **`PITCH`（App::Part）** の Placement がピッチ角。
コンテナの原点を回転軸 (0, 4, 120) に置いてあるので、
**プロパティエディタで Angle を変えるだけで軸まわりに正しく回る**
（Axis は X のまま）。マクロからは:

```python
exec(open("C:/Users/user/Documents/Free_cad/assembly_link.FCMacro.py",
          encoding="utf-8").read())
set_pitch(-12)     # 負角 = 前傾（見下ろし）
verify()           # マクロ生成版と位置が一致しているか照合
```

| ピッチ | 光軸 Z | カメラ前端 Y |
|---|---|---|
| −20° | 159.59 | 48.36 |
| −12° | 162.99 | 41.14 |
| 0° | 166.50 | 29.00 |
| +20° | 167.80 | 16.55 |

ツリー構成:

```
ASSEMBLY
 ├ TOWER          -> parts/01_TOWER.FCStd    の Body   (静止)
 ├ ADJUSTER       -> parts/02_ADJUSTER.FCStd の Body   (静止)
 ├ REF_BoltPivot / REF_BoltBase×4                      (静止)
 └ PITCH  (App::Part / 原点 = 回転軸。ここの Angle がピッチ角)
    └ PITCH_LOCAL (App::Part / 回転軸ぶんのオフセット)
       ├ CAM_MOUNT -> parts/03_CAM_MOUNT.FCStd の Body (回る)
       ├ REF_D455                                      (回る)
       └ REF_BoltClamp / REF_BoltCamL / REF_BoltCamR    (回る)
```

### 11.4 ハマった 3 点（同じことを繰り返さないために）

1. **外部リンクは、組立ドキュメントを一度保存してからでないと張れない。**
   未保存だと相対パスを解決できず、`LinkedObject` に代入しても `None` のままになる。
   しかも例外は出ずに静かに失敗する。`build()` では代入後に `None` チェックを入れてある
2. **`LinkTransform` は必ず `True` にする。** 既定の `False` はリンク先自身の
   Placement を「置き換える」ので、絶対座標の Placement を持つ参照ソリッド
   （D455 外形やボルト）が原点付近に飛ぶ
3. **`App::Link` に `getGlobalPlacement()` は無い。** コンテナの入れ子を含めた
   絶対位置が要るときは `InList` を辿って親の Placement を自前で合成する
   （`_ancestor_placement()`）

### 11.5 照合機能

`verify()` は、ピッチ 0° のときの `ASSEMBLY` と、マクロ生成版 `タワー.FCStd` の
各部品を **バウンディングボックス・重心・体積** で照合する。現在は全項目一致
（重心ずれ 0.000000 mm / 体積差 0 mm³）。

> 曲面のバウンディングボックスはテセレーション精度で 0.02 mm ほど揺れるので、
> BB は 0.05 mm 許容、位置の一致判定は重心で見ている。

### 11.6 注意

- **部品ファイルを移動・改名しない。** リンクが切れる
- `REF_`（D455 外形・締結部品）は**静的な参考形状**。部品側で穴位置を動かしたら
  `jetracer_d455_mount.FCMacro.py` を回して作り直すこと
- **`jetracer_d455_mount.FCMacro.py` を再実行すると `タワー.FCStd` が作り直され、
  そこから `split_parts.FCMacro.py` を回すと部品ファイルが上書きされる。**
  運用 (B) で部品ファイルを直接編集し始めたら、このルートは使わないこと
  （使う場合は `parts/_backup/` に退避されるので、そこから戻せる）
- 部品ファイルを開いたまま長時間作業していると、メモリ上のドキュメントが
  壊れることがあった（最初のリンク張り失敗の巻き添え）。挙動が怪しくなったら
  一度全部閉じてディスクから開き直すこと。**ディスク上のファイルは無事だった**

---

## 12. 実測タワー (50 × 24 × 70) への作り直し（rev.4 / 2026-09-09）

### 12.1 きっかけ

ユーザーが実機を採寸し、`parts/01_TOWER.FCStd` でタワーを
**60 × 30 × 100 → 50 × 24 × 70** に作り直した。加えて要求が 2 つ出た。

- ADJUSTER がタワーより大きいので **footprint を揃える**
- **全体高さを 160 mm 以内**に収める

### 12.2 何が引っかかったか

ADJUSTER のベース板は、締結部品から逆算すると 62.6 × 37.9 が必要だった。
効いていた制約はこの 2 本。

```
ベース幅   = 2 × (頬板外面 + M3座金R + 余白)                      → 62.6 必要
ベース奥行 = 2 × (回転軸Y + ARC_R·sin(SPAN) + クランプ座金R + 余白) → 37.9 必要
```

24 mm の奥行に収めるには **円弧半径（クランプの腕）を削る**しかなく、
そのままでは保持トルクが落ちる。パラメータを解き直した。

| | rev.3 | rev.4 | 理由 |
|---|---|---|---|
| `TAB_W` | 20 | **14** | ベース幅を 50 に収める |
| `CHEEK_T` | 6 | **5** | 同上 |
| `TAB_T` | 14 | **9** | 回転軸 Y を下げてベース奥行を 24 に収める |
| `ARC_R` | 16 | **8.5** | 同上 |
| clamp/pivot 座金 | OD14 | **OD12** | 24 mm 奥行に OD14 は入らない |
| clamp/pivot トルク | 0.80 N·m | **0.70 N·m** | OD12 の座面圧を 10 MPa 以下に保つ |
| base 座金 / トルク | OD12 / 0.35 | **OD9 / 0.30** | ベース幅 50 に収める + 座面圧 |

`PIV_Y` / `PIV_Z` / `BASE_HW` / `BASE_HD` は**ハードコードをやめて全部 DERIVED に移した**。

```python
BASE_HW = TOWER_W / 2          # ベースはタワーに揃える。はみ出させない
BASE_HD = TOWER_D / 2
PIV_Y   = -PLATE_T/2 + TAB_T/2                     # タブ厚の中心
PIV_Z   = BASE_Z1 + BASE_CLEAR + hypot(TAB_T/2, TAB_END_R)   # タブ下端の角がベースに当たらない最小
```

### 12.3 結果

```
BASE       50.0 (X) x 24.0 (Y) x 6.0   Z 70.0..76.0   ← タワー 50 x 24 と一致
CHEEK      X 7.30..12.30  Y -8.91..11.91  Z 76.0..102.3
FIX        X 18.30  Y 6.00   (タワー縁あき X 4.60 / Y 3.90)
CAM_Z0     108.3   光軸 Z 122.8   PLATE Z 108.3..145.3
全体高さ   145.3 mm  (要求 160 以内 / 余裕 14.7)
```

| ボディ | rev.3 | rev.4 |
|---|---|---|
| TOWER | 165.2 g | **50.3 g** |
| ADJUSTER | 35.3 g | **15.1 g** |
| CAM_MOUNT | 64.7 g | **55.4 g** |

検証は全項目 `ALL OK`。主な数字:

| 項目 | 値 | 判定 |
|---|---|---|
| クランプ保持トルク / 64 G 要求 | 7733 / 3929 N·mm | 安全率 **1.97** |
| clamp 座面圧 | 9.0 MPa | ≤ 10 |
| base 座面圧 | 9.2 MPa | ≤ 10 |
| 頬板/タブ接触面圧 | 5.4 MPa | ≤ 10 |
| タブ首部の層間応力 | 11.7 MPa | ≤ 15（PETG 層間 20-25） |
| ワッシャ座面 / 締結部品干渉 | 全ピッチ角で 0 | OK |

安全率は 2.74 → 1.97 に下がった（腕が 16 → 8.5 mm）。要求は満たすが、
**クランプの締め忘れが即座に効く設計になった**ので組付け時の増し締めを徹底すること。

### 12.4 新設した検証項目

- `頬板がベース奥行に収まる` — 円弧の振れ + 座金が footprint を超えたら NG
- `全体高さ <= MAX_TOTAL_H (160)` — タワー底面基準
- `ADJUSTER ベースがタワー footprint と一致`
- `タブ首部の層間応力 <= 15 MPa` — 積層が引張を受ける向きなので層間強度で見る

### 12.5 タワー側で直したこと

| 内容 | 前 | 後 |
|---|---|---|
| ポケット用スケッチ `Sketch001` の高さ | 40 | **50**（36 × 50、下辺は Z=0） |
| 同スケッチの未拘束 2 自由度 | 位置が浮いていた | `PointOnObject` + `Symmetric` を追加して**完全拘束** |
| マシン固定穴 `Sketch002` ×6 | Ø4 / Y=±10（側面に抜けて残り肉 0.00） | **Ø3.4（M3バカ穴）/ Y=±7**（端から中心 5.00 / 残り肉 3.30） |
| M3 インサート下穴 ×4 | X=±23.8, Y=±10（縁あき −0.90） | **X=±18.3, Y=±6**（縁あき 4.60 / 3.90） |

> `Sketch001` の高さを 40 → 50 にしたとき、未拘束の 2 自由度のせいで
> **ソルバが矩形ごと 2.83 mm 下にずらした**。寸法を変える前に完全拘束か
> 確認すること。FreeCAD の `RedundantConstraints` は **1 始まり**の
> インデックスを返す（GUI の「Constraint5」表記と同じ）ので、
> `delConstraint()` に渡すときは 1 引くこと。

### 12.6 パイプラインの改善

- マクロがマスター文書を **FileName で引き当てる**ようになった
  （開き直すと内部名が `Unnamed` → `タワー` に変わるため）
- タワー本体の Body を名前ではなく「ADJUSTER / CAM_MOUNT 以外の Body」で拾う
- `split_parts.check_fit()` と `assembly_link` の**回転軸をハードコードせず**、
  `02_ADJUSTER.FCStd` の `Sk_A5_PivotArc` の円から読むようにした
- `assembly_link.verify()` の位置判定を**重心と体積**で行うように変更
  （曲面の BB はテセレーション精度で 0.05 mm 揺れるため、BB は参考値扱い）

### 12.7 今回の作業手順（同じことをするとき）

1. `parts/01_TOWER.FCStd` を GUI で編集 → 保存
2. マスター `タワー.FCStd` の Body を削除し、01_TOWER の Body を
   `copyObject(src, True)` で履歴ごとコピー
3. `jetracer_d455_mount.FCMacro.py` の `TOWER_W/D/TOP_Z` を実測値に更新して実行
4. `split_parts.FCMacro.py` で部品ファイルを作り直す（`parts/_backup/` に退避される）
5. `assembly_link.FCMacro.py` の `build()` でリンク組立を作り直す

> `copyObject` は GUI ディスパッチがタイムアウトすると**何も起きずに終わる**ことがある。
> 実行後は必ず BoundBox で入れ替わったか確認すること。

---

## 13. 締結部品どうしの干渉（rev.5 / 2026-09-10）

### 13.1 見逃していたバグ

ユーザーが側面図で気づいた。**回転軸ボルトとクランプボルトのワッシャが重なっていた。**

```
中心距離 ARC_R = 8.5 mm
ワッシャ OD12 (半径 6) × 2 -> 必要な最小距離 12 mm
実測の食い込み = 40.85 mm3
```

rev.4 の検証は「締結部品 **と他部品**」の干渉しか見ておらず、
**締結部品どうし**を見ていなかった。ARC_R を 24 mm 奥行に収めるために
8.5 まで詰めたときに、この制約を考慮に入れていなかったのが原因。

### 13.2 直したこと

**円弧半径に下限を設けた。**

```python
ARC_R_MIN = 回転軸座金R + クランプ座金R + FASTENER_CLEAR   # = 6 + 6 + 1 = 13
ARC_R     = max(ARC_R_WANT, ARC_R_MIN)
```

半径を 8.5 → 13 に広げると、こんどはクランプ穴の Y 振れ
(`ARC_R·sin(SPAN)`) がベース奥行 24 mm を超える。そこで
**振り角のほうを自動で落とす**ようにした。

```python
_span_room = BASE_HD - PIV_Y - クランプ座金R - SEAT_MARGIN
ARC_SPAN   = floor(min(ARC_SPAN_WANT, degrees(asin(_span_room / ARC_R))))
```

結果 **ピッチ範囲は ±20° → ±13°** になった。`ARC_SPAN_MIN`(=10) を
下回ったら NG を出すので、これ以上詰まったら気づける。

### 13.3 新設した検証項目

```python
# 全ピッチ角で、締結部品 8 本の総当たり (28 ペア) を common() で見る
check(_fast_pairs_ng == 0, '締結部品どうしが全ピッチ角で干渉しない')
check(ARC_R >= ARC_R_MIN,  '円弧半径 >= 下限')
check(ARC_SPAN_OK,         'ピッチ振り角 >= 下限')
```

### 13.4 rev.4 → rev.5 の差分

| | rev.4 | rev.5 |
|---|---|---|
| `ARC_R` | 8.5（干渉） | **13.0** |
| ピッチ範囲 | ±20°（成立していなかった） | **±13°** |
| 回転軸 Z | 86.32 | 86.32 |
| 頬板上端 | 102.3 | 106.8 |
| カメラ底面 | 108.3 | 112.3 |
| 光軸 Z | 122.8 | **126.8** |
| 全体高さ | 145.3 | **149.3**（要求 160 / 余裕 10.7） |
| クランプ保持トルク | 7733 N·mm | **10489 N·mm** |
| 安全率 (64 G) | 1.97 | **2.40** |
| ADJUSTER | 15.1 g | 16.3 g |
| CAM_MOUNT | 55.4 g | 56.1 g |

腕が伸びたぶん**保持トルクと安全率はむしろ改善した**。
高さは 4 mm 増えたが要求内に収まっている。

### 13.5 ピッチ ±13° で足りるか

足りる。光軸は Z 126.8、V-FOV 57°(半角 28.5°) なので、**ピッチ 0° でも**
画角下端が地面に当たる距離は `d = h / tan(28.5°) = h / 0.543`。
h はタワー底面の地上高 + 126.8 mm。

D455 の実効 Min-Z は 848×480 で 350 mm、640×360 で 260 mm なので、
**0° の時点で既に Min-Z 付近まで地面が入っている**。これ以上下を向けても
深度が取れる範囲は増えない（引き継ぎ書 3.1 節と同じ理屈）。
±13° は据え付け誤差の吸収と微調整には十分。

### 13.6 ±20° に戻したい場合

ベース奥行を広げるしかない。`BASE_HD` はいまタワーに揃えて 12 固定なので、
そこを緩める。

| ベース奥行 | タワーからのはみ出し | 得られる振り角 |
|---|---|---|
| 24 mm（現在） | 0 | **±13°** |
| 28 mm | 片側 2 mm | ±22° |
| 30 mm | 片側 3 mm | ±26° |

`BASE_HD = TOWER_D / 2.0` を `BASE_HD = 14.0` などに書き換えて再実行すれば
`ARC_SPAN` が自動で追従する。

### 13.7 教訓

**「A と B が当たらない」「B と C が当たらない」を確かめても、
A と C を確かめたことにはならない。** 干渉チェックは総当たりにすること。
今回は 8 本の締結部品の 28 ペアを全ピッチ角で見るようにした。

---

## 14. 一体版の追加と高さ確保（rev.6 / 2026-09-10）

### 14.1 要求

- 全体高さは 160 を少し超えてもよい。**低いほうが問題**（車体・バンパーが画角に入る）
- 一体版と分割版を**両方残す**。アセンブリは一体版で組む

### 14.2 高さの稼ぎ方

`BASE_T`（ベース板厚 / 一体版では台座の高さ）を **6 → 17 mm** にした。
ヨークを伸ばして稼ぐより、**台座を厚くして稼ぐほうが片持ち長が短くなる**。

| 上げ方 | 追加質量 | 頬板の f_n |
|---|---|---|
| ヨークを 11 mm 伸ばす | +2.9 g | 168 Hz |
| **台座を 11 mm 厚くする** | +26 g | **225 Hz** |

質量は増えるが、一体版では台座はタワーと連続した中実材なので継手が増えない。

```
光軸 Z    126.8 -> 137.8 mm   (+11)
全体高さ  149.3 -> 160.3 mm
```

`MAX_TOTAL_H` は 160 → 165 に緩めた。

### 14.3 ピッチ角は ±13° のままでよい

遮蔽物（車体・バンパー）が画角に入る問題に対しては、**俯角を深くするほど不利**。
下向きにするほど FOV 下端が手前を向き、バンパーが入りやすくなる。
つまりこのケースで効くのは高さだけで、±20° に戻す必要はない。

画角下端が水平から下向き `α = |ピッチ| + 28.5°` なので、
**高さ h の光軸から水平距離 L 先にある高さ Hb の遮蔽物が画角に入らない条件**は

```
h  >=  Hb + L * tan(α)
```

| ピッチ | α | tan α | L=100 mm のとき許せる Hb |
|---|---|---|---|
| 0° | 28.5° | 0.543 | h − 54 |
| −6.5° | 35.0° | 0.700 | h − 70 |
| −13° | 41.5° | 0.885 | h − 89 |

**h はタワー底面の地上高 + 137.8 mm。** 実際に足りているかは
「バンパー上端の地上高」と「カメラからバンパーまでの水平距離」を測れば上式で判定できる。
足りなければ `BASE_T` を増やすだけで追従する。

### 14.4 一体版 04_TOWER_ADJ

`merge_tower_adjuster.FCMacro.py` を新設。
**01_TOWER と 02_ADJUSTER のスケッチをそのまま流用**して 1 つの Body に合成する。
パラメータを書き写していないので二重管理にならない。分割版で寸法を変えたら
この macro を回し直すだけでよい。

```
parts/04_TOWER_ADJ.FCStd   66279.7 mm3 / 84.2 g / 1 solid / スケッチ 7 枚すべて完全拘束
```

合成の中身:

1. 01_TOWER の Body を履歴ごとコピー
2. インサート下穴のフィーチャを落とす（一体なので不要）→ **Body.Tip を最終フィーチャに付け直す**
3. 02_ADJUSTER の Pad/Pocket を順に移植（`Pocket_A4_FixHoles` だけ飛ばす）

**検算**: 一体版と「分割版 2 つを fuse したもの」の差は

```
一体版にだけある材料 = 1060.7 mm3   (= M3バカ穴 4x617.3 + インサート下穴 4x443.4)
分割版にだけある材料 =    0.0 mm3
```

締結穴ぶんちょうど。それ以外の形状差はゼロ。

| | 分割版 | 一体版 |
|---|---|---|
| 部品点数 | TOWER + ADJUSTER | 1 個 |
| M3 x 12 + 熱圧入インサート | 4 組 | **不要** |
| 質量 | 50.3 + 32.5 = 82.8 g | 84.2 g |
| ベース座面のクリープ源 | 9.2 MPa | **無し** |

### 14.5 ハマった点: `Midplane` は FreeCAD 1.1 で読み出せない

フィーチャを移植したら、**頬板が片側にしか伸びていなかった**（一体版と分割版で
9256 mm3 の差）。原因は対称押し出しの扱い。

```
FreeCAD 1.1 では実体が SideType ('One side' / 'Two sides' / 'Symmetric')。
旧 Midplane は書き込みには効くが、保存してから読むと常に False を返す。
-> Midplane だけコピーすると Symmetric が落ちて片側だけ伸びる。
```

`SideType` をコピー対象に追加して解決。**フィーチャを複製するときは
`Midplane` ではなく `SideType` を見ること。**

### 14.6 アセンブリの切り替え

`assembly_link.FCMacro.py` の `MERGED` フラグ 1 つで組み替えられる。

```python
MERGED = True      # True: 04_TOWER_ADJ + 03_CAM_MOUNT
                   # False: 01_TOWER + 02_ADJUSTER + 03_CAM_MOUNT
```

`STATIC` / `REF_SRC` も自動で切り替わる。CAM_MOUNT (03) は**両方で共通**。
ヨークの寸法を一切変えていないので、どちらの組み合わせでも同じものが使える。

現在のアセンブリは一体版。照合は重心・体積とも完全一致で ALL OK。

### 14.7 MCP 経由の `copyObject` は時々黙って止まる

`nd.copyObject(src, True)` が GUI ディスパッチのタイムアウトで
**例外も出さずに何もせず終わる**ことが複数回あった。
実行後は必ず BoundBox か Volume で結果を確認すること。
FreeCAD のマクロコンソールから直接動かす場合はこの問題は出ない。

---

## 15. タブ幅拡大と造形性の検証（rev.7 / 2026-09-10）

### 15.1 ユーザーの変更を取り込み

`03_CAM_MOUNT` で**上端リブの奥行を 8 -> 17 mm** に深くしたのを macro 側に採用。
前後曲げの `I_zz` が 3126 -> 13779 mm⁴（4.4 倍）。追加 10.7 g。

### 15.2 タブ幅 14 -> 24 mm

リブを深くしたぶん荷重が首に集中するので、**タブ首部の層間応力**が設計上
いちばん厳しい数字になっていた。首部の断面係数は

```
Z = TAB_W * TAB_T^2 / 6
```

`TAB_T` はピッチ範囲に縛られて増やせない（`PIV_Y = -PLATE_T/2 + TAB_T/2` が
円弧の Y 振れを圧迫する）ので、**残ったレバーは `TAB_W` だけ**。

| | rev.6 | rev.7 |
|---|---|---|
| `TAB_W` | 14 | **24** |
| 首部の断面係数 | 189 mm³ | **324 mm³** |
| 首部の層間応力 (64 G) | 11.4 MPa | **7.3 MPa** |
| `TAB_END_R` (= TAB_W/2) | 7 | 12 |
| 光軸 Z | 137.8 | **142.3** |
| 全体高さ | 160.3 | **164.8**（上限 165） |
| 安全率 (クランプ) | 2.40 | 2.20 |
| CAM_MOUNT | 66.8 g | 71.9 g |

`TAB_END_R` が大きくなると回転軸が上がるので、高さも 4.5 mm 増える。
「低いほうが問題」という要求とは方向が一致しているので都合がよい。

### 15.3 代償: 分割版が成立しなくなった

タブが太ると頬板も外へ広がり、**M3 の座金が頬板とベース縁の間に入らなくなる**。

```
分割版が成立する上限 TAB_W = 2*(BASE_HW - 2*(M3座金R + 余白) - TAB_FIT - CHEEK_T)
                          = 2*(25 - 2*(4.5+1.5) - 0.3 - 5) = 15.4 mm
現在の TAB_W = 24.0  ->  分割版は成立しない
```

macro に `FIX_FITS` を入れ、成立しないときは**取付穴そのものを作らない**
（= 一体版 04 専用の構成になる）ようにした。検証は NG ではなく状況表示を出す。

```
[--] 分割版は成立しない (TAB_W 24.0 > 上限 15.4)。一体版 04 のみ有効
```

分割版を残したいなら `TAB_W <= 15.4` に戻すこと。ただし首部の応力は
11.4 -> 10.4 MPa 程度にしか下がらず、効果は小さい。
**TAB_W=14 の分割版一式は `parts/_backup/` に残してある。**

なお一体版と分割版を fuse したものの差は **0.00 / 0.00 mm³**（完全一致）。
この構成では締結穴が両方とも存在しないため。

### 15.4 造形性の検証（新設）

面ごとに外向き法線を求め、造形方向に対して 45° を超えて下を向く面を洗い出す
スクリプトで判定した。

**03_CAM_MOUNT — プレート裏面をベッド / 造形方向 +Y**

```
ベッド接地   5908 mm2 (1面)          ← 接地が広く反りにくい
ブリッジ     Ø4.5 x2 (回転軸・クランプの横穴)
★要サポート  0 面
```

タブのボス・シェルフ・上端リブがすべて +Y 側に揃っているので、
**サポートは一切不要**。

**04_TOWER_ADJ — タワー底面をベッド / 造形方向 +Z**

```
ベッド接地   336 mm2 (2面 = 脚の底 7x24 x2)
ブリッジ     Ø30.5 x2 (円弧長穴の外壁) / Ø21.5 x2 (内壁) / Ø4.5 x4 / Ø3.4 x12
★要サポート  1面  area=864 mm2  高さ=50.00
```

要サポートの 1 面は **コの字スロットの天井（36 x 24 = 864 mm²）**。
これは元のタワー単体の時点で存在していたもので、一体化で増えたものではない。
36 mm のブリッジなので、ブリッジ設定を詰めればサポート無しでも出る。

> **注意: 04 は接地面積 336 mm² に対して高さ 122 mm。**
> アスペクト比が悪く、印刷中に倒れる/剥がれるリスクがある。
> **ブリム（5〜8 mm）を必ず付けること。**

### 15.5 ハマった点

1. **最終フィーチャを消すと `Body.Tip` が None になる。** そのまま
   `Shape` を触ると `RuntimeError: shape is invalid`。
   取付穴を作らない分岐を入れたときにこれを踏んだ。残っている最後の
   PartDesign フィーチャに `Tip` を付け直すこと（merge 側でも同じ罠）
2. **`Midplane` は「コピーしてはいけない」。** 14.5 節で `SideType` を
   コピー対象に足したが、その**後ろに `Midplane` が残っていた**ため、
   読み出し値の `False` が `SideType` を `'One side'` に戻していた。
   コピーするのは `SideType` だけにする
3. **造形性判定でトリム外の点を拾っていた。** 面のパラメータ範囲を
   単純に等分割すると、面に開いた穴（シェルフやリブの付け根）の位置も
   サンプルしてしまい、法線の内外判定が反転する。
   `Face.isPartOfDomain(u, v)` で domain 内か必ず確かめること。
   平面は法線が一定なので**有効点 1 つで判定すれば十分**（総当たりは重すぎて
   GUI がタイムアウトする）

---

## 16. 頬板のはみ出し修正（rev.8 / 2026-09-10）

### 16.1 症状

ユーザーが `04_TOWER_ADJ` で `Pad_A2_CheekBlock` の押し出しを 34.6 -> 40 にし、
頬板プロファイルの Y を -10..13 に広げたところ、**頬板がタワー前面から
1 mm はみ出した**。

```
04_TOWER_ADJ  body BB  Y = -12 .. 13     ← タワーは Y ±12。+Y が 1 mm 超過
Pad len=40 (X ±20)                        ← X は ±25 内なので問題なし
```

### 16.2 正しい値

頬板 Y の下限は「全ピッチ角でワッシャ外周が母材に載る」条件で決まる。

```
CHEEK_Y1_MIN = PIV_Y + ARC_R*sin(SPAN) + クランプ座金R + SEAT_MARGIN
             = 1.5 + 13*sin(13°) + 6 + 1.5 = 11.925
CHEEK_Y0_MAX = min(PIV_Y - ARC_R*sin(SPAN) - 座金R, PIV_Y - 軸座金R) - SEAT_MARGIN
             = -8.925
```

上限は footprint（タワー ±12）。したがって **Y = -12 .. +12 が正解**
（最小要求 11.925 を満たし、かつ面一）。

### 16.3 macro 側の変更

最小値ぴったりにする理由が無いので、**footprint いっぱいまで広げて剛性を稼ぐ**
方針に変更した。`BASE_HD` でクランプするので、はみ出しは構造的に起きなくなる。

```python
CHEEK_Y1_MIN = ...   # 座面の要求（下限）
CHEEK_Y0_MAX = ...
CHEEK_Y1 = BASE_HD   # ★ footprint いっぱい。ここを超えると横にはみ出す
CHEEK_Y0 = -BASE_HD
```

押し出し 40 も採用し、`CHEEK_T` を 5 -> **7.7** にした
（`2*(TAB_W/2 + TAB_FIT + CHEEK_T) = 2*(12+0.3+7.7) = 40`）。

### 16.4 新設した検査

```python
check(_ovr <= 1e-6, 'ADJUSTER がタワー footprint からはみ出さない (最大 %+.2f mm)')
```

各ボディのバウンディングボックスをタワー footprint と比較する。
今回のような「横へのはみ出し」は今後この検査で必ず引っかかる。

### 16.5 もう 1 つ出た不整合: ギャップポケットの取り残し

頬板の Y を広げたあと、**頬板の間を抜くポケット `Sk_A3_Gap` が古い寸法のまま**
だったため、背面側に 1.08 mm の橋（934.5 mm³）が残って 2 枚の頬板が繋がっていた。

```
Sk_A3_Gap  Y -10.924 .. 13.924   ← 旧 CHEEK_Y0/Y1 ±2 のまま
頬板       Y -12 .. 12           ← 新しい値
-> Y -12..-10.92 に材料が残る
```

`Sk_A3_Gap` を Y -14..14（= 新 CHEEK_Y0/Y1 ±2）に直して解消。

> **教訓: 頬板の Y を変えたら、ギャップポケットの Y も一緒に変える。**
> macro では両方 `CHEEK_Y0/Y1` から導出しているので自動で追従するが、
> GUI で片方だけ触ると取り残しが出る。形状が繋がっていても
> 体積比較（一体版 vs 分割版の fuse）をすれば必ず見つかる。

### 16.6 結果

```
04_TOWER_ADJ   vol=72591.5 mm3 / 92.2 g / 1 solid / valid
BB             X -25..25  Y -12..12  Z 0..122.316    はみ出し X +0.00 / Y +0.00
一体版 vs 分割版 fuse  差 0.00 / 0.00 mm3            完全一致
```

| | rev.7 | rev.8 |
|---|---|---|
| `CHEEK_T` | 5.0 | **7.7** |
| 頬板 Y | -8.92..11.92 | **-12..12** |
| ADJUSTER | 34.5 g | **41.3 g** |
| 04_TOWER_ADJ | 85.4 g | **92.2 g** |

造形性は変わらず。

```
04_TOWER_ADJ  ベッド接地 336 mm2 / ブリッジ 22箇所 (最大 Ø30.5)
              ★要サポート 1面 = コの字スロット天井 864 mm2 (36 mm ブリッジ)
```

検証は全項目 ALL OK。

---

## 17. タブを厚くする（rev.9 / 2026-09-10）

### 17.1 なぜそのままでは厚くできなかったか

`TAB_T`（タブの Y 厚 = 頬板が掴む面の幅）は **rev.8 まで従属値**だった。

```
PIV_Y = -PLATE_T/2 + TAB_T/2
```

タブの裏面はプレートの裏面と面一でなければならない（印刷でベッドに接する面。
ここより後ろに出っ張ると宙に浮く）。そのため `TAB_T` を増やすと
**回転軸が前へ出て、ピッチ範囲を食いつぶす**。

```
制約:  PIV_Y + ARC_R*sin(SPAN) + クランプ座金R + SEAT_MARGIN <= BASE_HD(12)
  TAB_T =  9 -> PIV_Y = 1.5 -> ±13°
  TAB_T = 12 -> PIV_Y = 3.0 -> ±6.6°   ← ARC_SPAN_MIN(10) を割る
```

### 17.2 直し方: PIV_Y をパラメータに昇格

依存の向きを逆にした。**`PIV_Y` を決めてから、プレートの Y 位置を逆算する。**

```python
PIV_Y_WANT = 1.5                      # ここがピッチ範囲を決める
PIV_Y      = PIV_Y_WANT
PLATE_Y0   = PIV_Y - TAB_T / 2.0      # プレート裏面 = タブ裏面 = ベッド面
PLATE_Y1   = PLATE_Y0 + PLATE_T       # プレート前面 = カメラ背面
PLATE_YM   = (PLATE_Y0 + PLATE_Y1)/2  # 対称パッドの基準
CAM_Y0     = PLATE_Y1
```

これで `TAB_T` を増やしても `PIV_Y` は動かず、**ピッチ範囲が減らない**。
代わりにプレート（とカメラ）が後ろへずれる。

```
TAB_T  9 -> 14
PLATE_Y0   -3.0 -> -5.5     (プレート裏面が 2.5 mm 後退)
CAM_Y0      3.0 ->  0.5     (カメラ背面が 2.5 mm 後退)
カメラ前端 29.0 -> 26.5
```

**カメラが 2.5 mm タワー側に寄る**ので、オーバーハングのモーメントはむしろ減る。
配置バランスへの影響はこの 2.5 mm だけで、光軸高さもピッチ範囲も変わらない。

### 17.3 効果

| | rev.8 | rev.9 |
|---|---|---|
| `TAB_T` | 9.0 | **14.0** |
| タブ首部の断面係数 | 324 mm³ | **784 mm³** |
| **タブ首部の層間応力 (64 G)** | 7.3 MPa | **3.1 MPa** |
| 頬板/タブ接触の面圧 | 5.4 MPa | **3.5 MPa** |
| ピッチ範囲 | ±13° | **±13°**（不変） |
| 光軸 Z | 142.3 | 143.4 |
| 全体高さ | 164.8 | **165.9** |
| 安全率 (クランプ) | 2.20 | 2.14 |
| CAM_MOUNT | 71.9 g | 76.4 g |
| 04_TOWER_ADJ | 92.2 g | 92.7 g |

高さが 1.1 mm 増えたのは、タブ断面の対角半径
`_rc = hypot(TAB_T/2, TAB_END_R)` が 12.82 -> 13.89 になり、
タブ下端がベース板に当たらない最小の回転軸高さが上がったため。
`MAX_TOTAL_H` は 165 -> **170** に緩めた（「低いほうが問題」との判断）。

### 17.4 設計上いちばん厳しい数字が入れ替わった

```
rev.7  タブ首部 7.3 MPa   ← 最厳
rev.9  base ワッシャ座面 9.2 MPa / clamp ワッシャ座面 9.0 MPa  ← 最厳
       タブ首部は 3.1 MPa まで下がった
```

座面圧はどちらも `CREEP_LIMIT = 10 MPa` に対して 9 前後。これ以上詰めるなら
ワッシャ外径を上げる（= footprint を食う）かトルクを下げる（= 保持力が減る）
のどちらかで、いまはバランスが取れている位置にある。

### 17.5 パラメータの依存関係（現状のまとめ）

```
TOWER_W/D/TOP_Z (実測)
   └ BASE_HW/HD = footprint。ここを超えると横にはみ出す
        └ CHEEK_Y0/Y1 = ±BASE_HD             (面一まで広げて剛性を稼ぐ)
        └ ARC_SPAN = asin((BASE_HD - PIV_Y - 座金R - 余白) / ARC_R)
PIV_Y_WANT ──> PIV_Y ──> PLATE_Y0/Y1/YM ──> CAM_Y0
                 └ ARC_SPAN にも効く（小さいほどピッチが広い）
TAB_T ──> PLATE_Y0 (後退量) と 首部断面係数・接触面圧
TAB_W ──> CHEEK_XI/XO ──> 分割版の成立可否 (FIX_FITS)
ARC_R = max(ARC_R_WANT, 座金R + 座金R + FASTENER_CLEAR)
BASE_T ──> 全体高さの調整つまみ
```

---

## 18. 全体高さ 170 mm へ（rev.10 / 2026-09-10）

`BASE_T`（台座厚）**17.0 -> 21.0** の 1 パラメータのみ変更。

```
BASE       50.0 x 24.0 x 21.0   Z 70.0..91.0
CHEEK      X 12.30..20.00  Y -12.00..12.00  Z 91.0..127.4
CAM_Z0     132.9   光軸 Z 147.4   PLATE Z 132.9..169.9
全体高さ   169.89 mm   (上限 170)
```

| | rev.9 | rev.10 |
|---|---|---|
| `BASE_T` | 17.0 | **21.0** |
| 光軸 Z | 143.4 | **147.4** |
| 全体高さ | 165.9 | **169.9** |
| ADJUSTER | 41.8 g | 47.9 g |
| 04_TOWER_ADJ | 92.7 g | **98.8 g** |
| CAM_MOUNT | 76.4 g | 76.4 g（不変） |

ピッチ範囲・タブ寸法・座面圧・安全率はすべて不変（台座を厚くしただけなので、
その上の寸法連鎖は平行移動するだけ）。検証は全項目 ALL OK。
一体版と分割版 fuse の差は 0.00 / 0.00 mm³。

### 18.1 固有振動数への影響

高さを上げると片持ち長が伸びるので `f ∝ L^-1.5` で下がる。ただし
**台座を厚くする方式は、伸びるのが「剛体部分」なので影響が小さい**。

| | rev.9 (BASE_T=17) | rev.10 (BASE_T=21) |
|---|---|---|
| 頬板の片持ち長 (台座上面→光軸) | 56.4 mm | 56.4 mm（不変） |
| タワーの片持ち長 (底面→光軸) | 143.4 mm | 147.4 mm |
| タワーの f_n（概算） | 約 178 Hz | 約 171 Hz |

目標 150 Hz に対してはまだ余裕がある。**これ以上上げる場合は
`BASE_T` を増やし続けるより、タワー自体の断面を太らせる方が効率が良い**
（タワーのコの字部が系の最弱点なので）。

### 18.2 高さを変えるときの手順

```python
# jetracer_d455_mount.FCMacro.py
BASE_T = 21.0     # ここだけ。全体高さは BASE_T + 148.9 になる
```

その後 macro -> split_parts -> merge -> assembly_link の順に回す。
`MAX_TOTAL_H`（現在 170）を超えると検証が NG を出す。

---

## 19. 回転軸を頬板の中心へ（rev.11 / 2026-09-10）

### 19.1 指摘: 穴が中心からずれている

ユーザーが頬板の外面を見て「回転軸と円弧長穴が中心にない。仕様か？」と指摘。
**仕様ではなく、設計の名残だった。**

```
頬板は Y -12..12 (中心 0) なのに PIV_Y = 1.5
```

rev.9 まで `PIV_Y` は従属値で、たまたま 1.5 になっていた。

```
旧: PIV_Y = -PLATE_T/2 + TAB_T/2 = -3 + 4.5 = 1.5
```

rev.9 でパラメータに昇格させたとき、**配置を変えないよう 1.5 を据え置いた**。
設計上の必然性は無い。

### 19.2 0 にすると副産物がある

`PIV_Y` は円弧長穴の Y 振れ余裕を直接食う。

```
_span_room = BASE_HD - PIV_Y - クランプ座金R - SEAT_MARGIN
  PIV_Y = 1.5 -> 3.0 mm -> ARC_SPAN = asin(3.0/13) = ±13°
  PIV_Y = 0   -> 4.5 mm -> ARC_SPAN = asin(4.5/13) = ±20°   ← 満額
```

つまり**中心に置くほうがピッチ範囲が広い**。

ただし ±20° にするとピッチ回転時のクリアランス確保でカメラが 2 mm 上がるので、
`BASE_T` を 21 -> 19 に戻して相殺した。**高さと光軸は据え置きのまま。**

### 19.3 結果

| | rev.10 | rev.11 |
|---|---|---|
| `PIV_Y` | 1.5 | **0.0**（頬板中心） |
| `BASE_T` | 21.0 | **19.0** |
| **ピッチ範囲** | ±13° | **±20°** |
| 光軸 Z | 147.4 | **147.4**（不変） |
| 全体高さ | 169.9 | **169.9**（不変） |
| カメラ背面 Y | -1.0 | **-2.5**（1.5 mm 後退） |
| 安全率 (クランプ) | 2.14 | 2.03 |
| ADJUSTER | 47.9 g | 44.6 g |
| 04_TOWER_ADJ | 98.8 g | **95.5 g** |
| CAM_MOUNT | 76.4 g | 77.2 g |

**高さ・光軸を変えずにピッチ範囲が満額に戻り、しかも軽くなった。**
代償はカメラが 1.5 mm タワー側に寄ることだけで、モーメントはむしろ減る。

安全率が 2.14 -> 2.03 に下がったのは、`PIV_Z` が 2 mm 下がって
重心までの腕が 40.5 -> 42.5 mm に伸びたため。目標 1.5 に対しては余裕がある。

検証は全項目 ALL OK。一体版と分割版 fuse の差 0.00 / 0.00 mm³、
footprint はみ出しゼロ、座面 OK、締結部品どうしの干渉ゼロ（±20° 全域）。

### 19.4 教訓

**従属値をパラメータに昇格させるときは、旧式が出していた値を
そのまま初期値にしない。** その値は旧式の都合で決まっていただけで、
新しい自由度の下では最適でないことがある。今回は 1.5 -> 0 にするだけで
ピッチ範囲が 1.5 倍になった。昇格させた時点で「この値は本当に最適か」を
一度問い直すこと。

---

## 20. 最終チェックと STL 書き出し（rev.12 / 2026-09-10）

### 20.1 嵌合すきま 0.3 -> 0.4 mm

`TAB_FIT` を 0.4 に変更。FDM は外寸が大きめ・内寸が小さめに出るので、
0.3 では寸法誤差 ±0.2 を吸収しきれず嵌らない恐れがあった。

```
頬板 内面 X = ±12.40    タブ X = ±12.00    -> 片側 0.40 mm (実測確認済み)
```

クランプは摩擦締結なので、締めれば頬板が 0.4 mm たわんでタブに密着する。
必要な力は約 91 N（頬板を片持ち 28.9 mm・断面 7.7×24 として `k=3EI/L³=227 N/mm`）で、
予圧 875 N の 10% 程度。保持力への影響は無視できる。

### 20.2 最終実測（ソリッドから直接測った値）

**D455 取付**

| 項目 | 実測 | 判定 |
|---|---|---|
| 背面 M4 | Ø4.5 軸=Y (±47.5, Z=147.392) ピッチ 95.0 | ✓ |
| M4 高さ | カメラ底面 132.892 から +14.5 = 背面の上下中心 | ✓ |
| プレート前面 | Y = -1.000 = カメラ背面と一致 | ✓ |
| シェルフ上面 | Z = 132.892 = カメラ底面と一致（3点支持） | ✓ |
| シェルフ X | -33..-22 / -12..12 / 22..33（USB-C 帯 \|X\|33-60 は開放） | ✓ |
| 上端リブ | Z 162.892 = カメラ上面 +1.0 | ✓ |
| CAM_MOUNT × D455 | 干渉 0.0000 mm³ | ✓ |

**ピッチ機構**

| 項目 | 実測 | 判定 |
|---|---|---|
| 回転軸 / クランプ | Ø4.5 (Y=0, Z=104.892) / (Y=0, Z=117.892) 距離 13.0 | ✓ |
| 円弧長穴 | 端 Y=±4.45 = 13·sin20° / 内外壁 Ø21.5・Ø30.5 | ✓ |
| 嵌合 | 頬板 ±12.40 / タブ ±12.00 -> 片側 0.40 | ✓ |
| 干渉（-20〜+20°、9点） | ADJ×CAM = ADJ×D455 = 0.0000 mm³ | ✓ |
| タブ下端の最小すきま | 2.222 mm（±20° 時） | ✓ |

**タワー（ユーザー実測値の保持）**

| 項目 | 実測 |
|---|---|
| 外形 / スロット / 脚厚 | 50×24×70 / 36×50 / 7.0 |
| マシン固定穴 | Ø3.4 ×6、Y=±7、Z=8/18/44 |
| 最小肉厚 | 3.30 mm（M3 穴〜側面）。他は 4.3 mm 以上 |

**全体**: 高さ 169.892 / 光軸 147.392 / ピッチ ±20° /
質量 04=95.5 g + 03=77.2 g = **172.7 g**（PETG 1.27）

### 20.3 STL

```
export/print/TOWER_ADJ.stl        15204 面  742.5 KB
export/print/CAM_MOUNT_D455.stl    4340 面  212.0 KB
```

弦偏差 0.01 mm でテセレーション。書き出し後に検査した結果:

| | ソリッド | 自己交差 | 非多様体 | 体積誤差 |
|---|---|---|---|---|
| TOWER_ADJ | True | False | False | +0.001% |
| CAM_MOUNT_D455 | True | False | False | +0.000% |

**印刷するのはこの 2 ファイルだけ。** `export/` 直下にある
`TOWER.stl` / `ADJUSTER_pitch.stl` は分割版時代の古い出力なので使わないこと。

### 20.4 印刷設定の注意

**TOWER_ADJ**（タワー底面をベッドに置いて立てる）
- ベッド接地は脚の底 7×24 が 2 枚で **336 mm² のみ**、部品高さ 125 mm。
  **ブリム 5〜8 mm を必ず付ける**（倒れ・剥がれ対策）
- コの字スロットの天井（Z=50、幅 36 mm）が唯一のブリッジ。
  ブリッジ設定のままで出るが、気になればスロット内にサポート
- 円弧長穴と回転軸穴は縦壁の横穴（Ø4.5 / スロット幅 4.5）。ブリッジで出る

**CAM_MOUNT_D455**（背面プレートの裏面をベッドに置いて寝かせる）
- ベッド接地 5956 mm²。**要サポート面ゼロ**
- ブリッジは回転軸・クランプの横穴 Ø4.5 が 2 箇所だけ

### 20.5 ねじ（市販品）

| 用途 | 本数 | ねじ | ワッシャ | ナット | トルク |
|---|---|---|---|---|---|
| 回転軸 | 1 | **M4 × 50** | 平 OD12 ×2 | M4 ナイロンナット | 0.7 N·m |
| クランプ | 1 | **M4 × 50** | 平 OD12 ×2 | M4 ナイロンナット | 0.7 N·m |
| D455 背面 | 2 | **M4 × 10**（12 以上は底突き） | 平 OD12 | — | 0.4 N·m |
| マシン固定 | 6 | M3（長さはマシン側の板厚次第） | 平 OD9 | M3 | 0.3 N·m |

M4×50 の根拠: 掴み厚 40.2（頬板外面 ±20.1）+ ワッシャ 1.0×2 + ナイロンナット 5.0
= **47.2 mm** 必要。M4×40 では届かない。

マシン固定 M3 はタワーを 50 mm 貫通する（脚 7 + スロット 36 + 脚 7）。
実際の長さはスロットに挟むマシン側部品の厚みで決まる。
